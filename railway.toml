[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
buildContext = "backend"

[deploy]
startCommand = "node scripts/generate-prisma-client.js --push && node db/seed.js && node app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
