[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install"

[deploy]
startCommand = "cd backend && node db/seed.js && node app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
